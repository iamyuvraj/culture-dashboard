export default function App() {
  const headingClasses = 'text-center text-3xl text-red-500 font-bold underline';
  
  return (
    <h1 className={headingClasses}>
      Vite + React + TS
    </h1>
  );
}
